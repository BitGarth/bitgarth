#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

"${SCRIPT_DIR}/e2e-build"

cd "${ROOT_DIR}"
node --test tests/e2e/helpers/playwright-config.test.mjs
exec npx playwright test "$@"
